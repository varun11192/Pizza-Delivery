<?xml version="1.0" encoding="utf-8"?><RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Logo -->
    <ImageView
        android:id="@+id/logoImageView"
        android:layout_width="wrap_content"
        android:layout_height="80dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/logo" />

    <!-- "Your Order Has Been Placed" heading -->
    <TextView
        android:id="@+id/orderPlacedTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/logoImageView"
        android:layout_centerHorizontal="true"
        android:text="Your Order Has Been Placed" />

    <!-- Order ID card view -->
    <androidx.cardview.widget.CardView
        android:id="@+id/orderIdCardView"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/orderPlacedTextView"
        android:layout_centerHorizontal="true"
        app:cardBackgroundColor="#ffffff"
        app:cardCornerRadius="8dp"
        app:cardElevation="4dp">

        <LinearLayout
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp">

            <TextView
                android:id="@+id/orderIdTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Order ID: 12345" />

            <!-- Second Image -->
            <ImageView
                android:id="@+id/secondImageView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:src="@drawable/order_image" />

            <!-- "Thank you for ordering with Pizzo" text -->
            <TextView
                android:id="@+id/thankYouTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Thank you for ordering with Pizzo!" />

            <!-- Total price -->
            <TextView
                android:id="@+id/totalPriceTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Total Price: $XX.XX" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

    <!-- Cart button -->
    <Button
        android:id="@+id/cartButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/orderIdCardView"
        android:layout_alignEnd="@+id/orderIdCardView"
        android:layout_marginTop="51dp"
        android:layout_marginEnd="56dp"
        android:text="Cart" />

</RelativeLayout>


